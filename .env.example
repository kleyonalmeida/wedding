# ════════════════════════════════════════════════════════════════════════════
# INSTRUÇÕES:
#   1. Copie este arquivo: cp .env.example .env
#   2. Preencha TODOS os valores no arquivo .env
#   3. NUNCA faça commit do arquivo .env (já está no .gitignore)
#
# Este arquivo fica na RAIZ do projeto e é lido pelo Docker Compose
# para interpolação de variáveis (${VAR}) no docker-compose.yml.
# ════════════════════════════════════════════════════════════════════════════

# ── PostgreSQL ────────────────────────────────────────────────────────────────
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# ── API — Origem do Flutter Web ──────────────────────────────────────────────
# Exemplo: https://casamento.seudominio.com.br
ALLOWED_ORIGIN=

# ── JWT ───────────────────────────────────────────────────────────────────────
# Gere um secret aleatório seguro (mínimo 32 caracteres):
#   Linux: openssl rand -base64 48
JWT_SECRET=
JWT_ISSUER=wedding-rsvp-api

# ── Admin ─────────────────────────────────────────────────────────────────────
ADMIN_USERNAME=

# Hash BCrypt da senha do admin. Gere com o script:
#   bash backend/scripts/generate-admin-hash.sh
ADMIN_PASSWORD_HASH=
